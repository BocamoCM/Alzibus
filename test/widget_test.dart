// Smoke test mínimo. La cobertura real del dominio Auth está en
// `test/domain/` y `test/application/`. Los tests de widgets de la UI
// llegarán cuando se migren las pages a casos de uso.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
